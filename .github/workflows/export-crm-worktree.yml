name: Export CRM Worktree

on:
  push:
    branches:
      - mvp/crm-multitenant

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: zip -qr /tmp/crm-worktree.zip . -x '.git/*' 'node_modules/*' 'dist/*' '.vercel/*'
      - uses: actions/upload-artifact@v4
        with:
          name: crm-worktree
          path: /tmp/crm-worktree.zip
          retention-days: 1
