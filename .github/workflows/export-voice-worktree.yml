name: Export Voice Worktree

on:
  push:
    branches:
      - mvp/voice-hardening

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: zip -qr /tmp/voice-worktree.zip . -x '.git/*' 'node_modules/*' 'dist/*' '.vercel/*'
      - uses: actions/upload-artifact@v4
        with:
          name: voice-worktree
          path: /tmp/voice-worktree.zip
          retention-days: 1
