name: Export Chatbot Worktree

on:
  push:
    branches:
      - mvp/chatbot-provider

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: zip -qr /tmp/chatbot-worktree.zip . -x '.git/*' 'node_modules/*' 'dist/*' '.vercel/*'
      - uses: actions/upload-artifact@v4
        with:
          name: chatbot-worktree
          path: /tmp/chatbot-worktree.zip
          retention-days: 1
