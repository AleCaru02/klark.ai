name: Temporary Twilio Bundle Readonly

on:
  push:
    branches:
      - ops/twilio-bundle-readonly-20260810
    paths:
      - .github/workflows/tmp-twilio-bundle-readonly.yml

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Invoke bundle readonly probe
        shell: bash
        run: |
          set -euo pipefail
          curl -sS 'https://ipazbzctivqquwndifxh.supabase.co/functions/v1/twilio-bundle-readonly?nonce=bundlecheck_20260810_a7f2c9'
